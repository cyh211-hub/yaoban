# 遥伴隐私说明（测试版）

更新：2026-09-13。本说明描述当前测试版设计和已经核对的实现边界；正式发布前会随实际发布构建再次审计。

## 权限用途

- **蓝牙**：识别并连接用户明确选择的已配对遥控器，读取设备状态；小米语音功能还通过其已适配的蓝牙语音通道接收声音。
- **输入监控**：接收遥控器 HID 输入，并在快捷键录入或必要的按键状态协调期间临时观察相关事件。
- **辅助功能**：向当前系统发送用户配置的键盘和鼠标事件。
- **管理员安装权限**：安装或更新“遥控器麦克风”虚拟声卡；启用 Apple 遥控器语音时还用于受限的本地后台采集组件。

空闲待命时，遥伴不建立普通键盘的全局文本监听。协调或录入通道启用时，系统可能送来同类型的其他事件；程序忽略无关普通按键，不把普通键盘输入拼接为文本，也不写入键盘文本日志。

## 语音与录音

- 小米遥控器语音来自已绑定遥控器的 ATVV 通道，不以 Mac 内建或 USB 麦克风作为隐式备用来源。
- Apple 遥控器语音当前通过 Apple PacketLogger 提供的系统级 HCI 捕获入口取得原始蓝牙数据，再由遥伴筛选绑定设备、重组和解码。底层诊断可能经过其他蓝牙设备流量，并非硬件层只采集当前遥控器。
- Apple 语音临时原始捕获文件存放在 root 私有目录；正常、异常结束和服务恢复时清理。没有网络上传或永久原始蓝牙历史。
- 原声试听录音默认关闭。用户主动启用后才保存 WAV、压缩音频和同步诊断，可在应用中清理；单次小米语音上限 60 秒。诊断日志和试听录音最多保留 7 天，当前容量上限分别为 10 MiB 和 20 MiB。

## 本地数据

遥伴在用户的应用支持目录保存按键设置、预设、设备绑定标识、恢复记录和受限诊断。设置与诊断目录使用仅限当前用户的权限；Apple 语音后台数据使用 root 私有位置。设备标识用于重新识别已绑定遥控器，不是密码学身份凭证。

## 第三方处理

遥伴负责按键映射、音频桥接和虚拟麦克风输出。语音识别、文字生成、智能体任务执行以及云端处理由用户选择的输入法、桌面智能体或办公软件负责，其数据处理受对应第三方隐私政策约束。

PacketLogger 属于 Apple 专有工具。遥伴不捆绑、镜像或重新分发它；用户从 Apple 官方渠道取得并接受 Apple 对应协议。

## 用户控制

- 可以暂停映射、停用当前设备或从菜单栏退出遥伴。
- 可以关闭原声试听保存并清理本应用保存的试听录音。
- 可以删除遥控器条目，同时保留已保存预设；也可以卸载应用和“遥控器麦克风”组件。
- 将虚拟麦克风设为系统默认输入会影响所有跟随系统默认值的应用；遥伴提供恢复先前输入设备的操作。

公开反馈时，请先移除设备地址、私人语音、输入内容和不相关日志。安全问题建议通过仓库的私密安全联系渠道报告；在该渠道建立前，不要把敏感信息提交到公开 Issue。
